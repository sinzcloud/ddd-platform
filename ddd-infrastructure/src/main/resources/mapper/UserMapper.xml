<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ddd.platform.infrastructure.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.ddd.platform.infrastructure.po.UserPO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="lock_status" property="lockStatus"/>
        <result column="fail_count" property="failCount"/>
        <result column="lock_time" property="lockTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, username, password, email, phone, status,
        lock_status, fail_count, lock_time,
        create_time, update_time, deleted
    </sql>

    <!-- 批量插入 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO sys_user (
        username, password, email, phone, status,
        lock_status, fail_count, lock_time, create_time, update_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.username}, #{item.password}, #{item.email}, #{item.phone}, #{item.status},
            #{item.lockStatus}, #{item.failCount}, #{item.lockTime},
            #{item.createTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

</mapper>