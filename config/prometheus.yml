global:
  scrape_interval: 15s

# 加载告警规则
rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: 'ddd-platform'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']